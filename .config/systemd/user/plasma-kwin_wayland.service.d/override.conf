[Service]
# Prefer the NVIDIA DRM device first on this hybrid laptop to avoid
# KWin/Wayland cross-GPU framebuffer regressions on the HDMI output.
Environment=KWIN_DRM_DEVICES=/dev/dri/card1:/dev/dri/card2
